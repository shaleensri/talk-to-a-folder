import { AppShell } from '@/components/layout/AppShell'

/**
 * Root page — renders the full app shell.
 * All routing is implicit via state (active folder, chat session).
 */
export default function Home() {
  return <AppShell />
}
